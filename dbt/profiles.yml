studioflow:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'studioflow') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'studioflow') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'studioflow') }}"
      schema: dbt_default
      threads: 4
